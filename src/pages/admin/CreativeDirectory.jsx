import { ExternalLink } from 'lucide-react'; import { useEffect, useState } from 'react'; import AdminLayout from '../../components/admin/AdminLayout'; import { AdminButton, AdminEmptyState, AdminNotice, AdminPageHeader, AdminSurface } from '../../components/admin/AdminUI'; import LoadingState from '../../components/LoadingState'; import { supabase } from '../../lib/supabaseClient';
export default function CreativeDirectory(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{supabase.from('creative_members').select('id,name,slug,role,short_bio,profile_image_url,skills,availability_status').eq('is_published',true).order('display_order',{ascending:true,nullsFirst:false}).then(({data,error})=>{setRows(data||[]);setError(error?.message||'');setLoading(false);});},[]);return <AdminLayout><AdminPageHeader eyebrow="Collective" title="Creative Directory" description="Browse approved creative profiles and their published work."/>{error&&<AdminNotice className="mb-5">{error}</AdminNotice>}{loading?<LoadingState label="Loading creatives"/>:rows.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.map(c=><AdminSurface key={c.id} as="article" className="flex min-w-0 flex-col gap-4"><div className="flex items-start gap-3">{c.profile_image_url?<img src={c.profile_image_url} alt="" className="h-14 w-14 shrink-0 rounded-md object-cover"/>:<div className="grid h-14 w-14 shrink-0 place-items-center rounded-md bg-white/[0.06] text-zinc-500">{c.name?.slice(0,1)}</div>}<div className="min-w-0"><h2 className="truncate font-semibold text-white">{c.name}</h2><p className="mt-1 text-sm text-zinc-400">{c.role}</p>{c.availability_status&&<p className="mt-1 text-xs text-zinc-500">{c.availability_status}</p>}</div></div>{c.short_bio&&<p className="text-sm leading-6 text-zinc-400">{c.short_bio}</p>}<AdminButton to={`/creatives/${c.slug}`} className="mt-auto w-fit"><ExternalLink size={15}/> View profile</AdminButton></AdminSurface>)}</div>:<AdminEmptyState title="No approved creatives yet" message="Approved creative profiles will appear here."/>}</AdminLayout>}
